

def __test_variable_augassign() {
    x = 5
    assert_eq(x -= 2, 3)
    assert_eq(x, 3)

    x -= 4
    assert_eq(x, -1)

    x += 5
    assert_eq(x, 4)
    assert_eq(x *= 5, 20)
    assert_eq(x, 20)
}
