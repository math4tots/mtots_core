def __test_cell() {
    cell = Cell('hello')
    another_cell_ref = cell

    assert_eq(cell.get(), 'hello')
    assert_eq(another_cell_ref.get(), 'hello')
    cell.set(12)
    assert_eq(cell.get(), 12)
    assert_eq(another_cell_ref.get(), 12)
}
