
def __test_extend() {
    mb = MutableBytes([])
    assert_eq(mb, MutableBytes([]))
    assert(mb != MutableBytes([1, 2, 3]))
    mb.extend([1, 2, 3])
    assert_eq(mb, MutableBytes([1, 2, 3]))
    mb.extend(mb)
    assert_eq(mb, MutableBytes([1, 2, 3, 1, 2, 3]))
}
