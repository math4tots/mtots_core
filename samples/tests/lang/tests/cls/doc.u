

class ClassWithDoc {
    # This is some class
    # with some docs
}

def __test_basic() {
    "Doc for Object should not be nil or empty"
    assert(Object.doc())

    assert_eq(ClassWithDoc.doc(), 'This is some class\nwith some docs\n')
}
